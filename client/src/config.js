export const appName = 'currency-watch';

export const API_BASE_URL = `${window.location.origin}/api`;
export const API_LOAD_EXCHANGES = `${API_BASE_URL}/exchanges`;
export const API_LOAD_CURRENCIES = `${API_BASE_URL}/currencies`;
export const API_LOAD_HISTORICAL = `${API_BASE_URL}/historical`;

export const IOSERVER = 'http://localhost:5000/';
